# Curation adapters

Un adapter registra cómo convertir una entrada evaluada en un artefacto reproducible: pin upstream, integración, archivos gestionados, checks y actualización. Sin adapter y piloto, una entrada no puede superar `pilot-ready`.
