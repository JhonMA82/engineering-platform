# SpeedPy application

Read `.engineering/project.json` and the root `GENTLE.md` before changing behavior.

- Keep business logic outside views and transport handlers.
- Use the existing service, repository and design-system boundaries.
- Add migrations for data changes and tests for regressions.
- Run the checks registered in `.engineering/materialization.json`.
- Consult `docs/boilerplates/speedpy-AGENTS.md` only when detailed upstream guidance is needed.
