diff --git a/app/navigators/navigationUtilities.ts b/app/navigators/navigationUtilities.ts
--- a/app/navigators/navigationUtilities.ts
+++ b/app/navigators/navigationUtilities.ts
@@ -67,8 +67,13 @@ export function useBackButtonHandler(canExit: (routeName: string) => boolean) {
         return false
       }

-      // grab the current route
-      const routeName = getActiveRouteName(navigationRef.getRootState())
+      // The navigation state can still be unavailable during startup.
+      const rootState = navigationRef.getRootState()
+      if (!rootState) {
+        return false
+      }
+
+      const routeName = getActiveRouteName(rootState)

       // are we allowed to exit?
       if (canExitRef.current(routeName)) {
