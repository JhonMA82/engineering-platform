# Ignite mobile app

Lee `../../.engineering/project.json` y `../../GENTLE.md` antes de cambiar código.

- Conserva la estructura Ignite dentro de `app/`.
- Reutiliza componentes, navegación y tokens existentes.
- No dupliques reglas de negocio del backend.
- Ejecuta `npm run compile`, `npm run lint:check` y `npm test -- --runInBand`.
- Valida permisos nativos, estados offline y accesibilidad antes de añadir una dependencia.
