# Add a boilerplate

Canonical alias for the curation runbook: the full procedure lives in
[`curate-boilerplate.md`](curate-boilerplate.md) (seven steps: read the old
entry, read the adapter, read the evidence, transform to the v1 contract,
validate, contract test, routing/composition scenario).

Start there. This file exists only so the §8 documentation set resolves by
its specified name; there is deliberately a single runbook, not two.
