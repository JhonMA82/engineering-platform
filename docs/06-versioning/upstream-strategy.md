# Upstream Strategy

upstream → mirror/vendor → integration → tests/evals → curated release. No desarrollar cambios internos en rama espejo. Cada release documenta commit upstream, cambios propios, incompatibilidades, migración y pruebas.
