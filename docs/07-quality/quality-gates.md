# Quality Gates

format → lint → typecheck → architecture → unit → integration → contracts → security → build.

Condicionales: DB→migration; API→OpenAPI diff; tenant→isolation; permission→matrix; UI→a11y/E2E; deploy→smoke. Done depende de gates.
