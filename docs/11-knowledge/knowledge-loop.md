# Knowledge Loop — ejemplo

Bug: invitaciones expiradas todavía podían aceptarse.

```text
reproduce
→ regression test
→ fix
→ verificar
→ knowledge entry
→ ¿afecta starter? sí
→ patch del auth/multitenancy pack
→ upgrade recipe
```

Si el problema era una regla exclusiva de un cliente, se queda en su repo.
