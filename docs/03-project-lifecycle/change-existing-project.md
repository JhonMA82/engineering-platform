# Flujo: cambio en proyecto existente

No repetir init. Leer manifest → clasificar feature/dependency/pack/upgrade/architecture → precheck → plan/migration → aplicar solo necesario → actualizar manifest → gates → ADR si cambia decisión fundamental.
