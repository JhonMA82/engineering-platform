# Flujo: incidente / bug

Mitigar → reproducir → regression test falla → fix → validar → causa raíz → clasificar alcance → knowledge si reusable → upgrade/security recipe si afecta proyectos existentes.
