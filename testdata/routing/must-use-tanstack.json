{
  "name": "must-use-tanstack",
  "intent": {
    "schema_version": 1,
    "name": "tanstack admin",
    "problem": "team decided on tanstack for the admin",
    "surfaces": [{ "kind": "web-admin", "access": "authenticated", "scope": "required_now" }],
    "technical_constraints": [{ "kind": "must-use", "value": "tanstack" }]
  },
  "expect": {
    "status": "resolved",
    "selected_recipe": "GP-02",
    "must_include_reasons": ["must-use tech tanstack"],
    "must_not_select": ["GP-01"]
  }
}
