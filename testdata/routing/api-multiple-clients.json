{
  "name": "api-multiple-clients",
  "intent": {
    "schema_version": 1,
    "name": "shared api",
    "problem": "web and mobile consume the same api",
    "surfaces": [
      { "kind": "api", "access": "authenticated", "scope": "required_now" },
      { "kind": "public-web", "access": "authenticated", "scope": "required_now" },
      { "kind": "mobile-native", "access": "authenticated", "scope": "required_now" }
    ],
    "data": { "persistence": "shared", "multi_user": true }
  },
  "expect": {
    "status": "resolved",
    "selected_recipe": "GP-06",
    "must_include_reasons": ["shared-backend"],
    "must_not_select": ["GP-02"]
  }
}
