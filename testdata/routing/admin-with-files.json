{
  "name": "admin-with-files",
  "intent": {
    "schema_version": 1,
    "name": "records with attachments",
    "problem": "staff manages records with file attachments and audit trail",
    "product_requirements": [
      { "id": "files", "description": "attach files to records" },
      { "id": "audit", "description": "audit trail of record changes" }
    ],
    "surfaces": [{ "kind": "web-admin", "access": "authenticated", "scope": "required_now" }]
  },
  "expect": {
    "status": "resolved",
    "selected_recipe": "GP-02",
    "must_include_reasons": ["covers required surface web-admin"],
    "must_not_select": ["GP-06"]
  }
}
