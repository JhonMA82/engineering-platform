#!/usr/bin/env sh
set -eu

# Resolve the launcher symlink so the global `eng` entry point can find its package.
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$(readlink -f -- "$0")")" && pwd -P)
exec python3 "$SCRIPT_DIR/scripts/eng.py" "$@"
