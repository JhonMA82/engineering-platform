# Curation evidence — fastapi

- Upstream: <https://github.com/fastapi/full-stack-fastapi-template>
- License: unverified (gap). The legacy catalog records no license
  observation for this entry and no evidence file was ever migrated, so the
  license MUST be confirmed during curation before any promotion.
- Pin: none. The legacy catalog records NO upstream commit for this entry,
  so no pin is declared here either — inventing one (a tag, a HEAD guess,
  or `v1.0.0`) is forbidden. A commit may be frozen only inside a real
  curation/pilot process, with evidence, and only then does the entry
  become materializable.
- Legacy review: `platform/boilerplates.json` entry `fastapi`
  (legacy_id `full-stack-fastapi-template`, alternative/pilot-ready, tier
  B, category python-api). Recovered by the legacy-identity repair
  migration as catalog knowledge; deliberately NOT promoted.
- Status: alternative / catalog-only (downgraded from the historical
  pilot-ready: without a pin there is nothing honest to pilot).
  Non-selectable: the composer eligibility gate requires pin+adapter, so
  this entry can never serve a surface until curation completes.
- Pilot: excluded — no pin exists to verify (see
  docs/decisions/migration-notes.md).
- Gaps (explicit): no pin; no adapter; license unverified; no pilot; never
  promoted automatically by migration alone.
