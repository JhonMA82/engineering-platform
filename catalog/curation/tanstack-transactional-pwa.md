# Curation evidence — tanstack-transactional-pwa

- Upstream: <https://github.com/JhonMA82/tanstack-transactional-pwa>
- License: unverified (gap). No license record was carried over from legacy,
  so the license MUST be confirmed on pilot before materializing for a
  real project.
- Pin: `v1.0.0` (declared, NOT verified as a tag). The repository IS publicly
  reachable (HEAD resolves, checked 2026-09-08) but advertises no `v1.0.0`
  tag, so the pin cannot be verified from here. Offline pilots repoint this
  id at a local fixture source; a network pilot must re-verify the pin first.
- Adapter: fetch+copy placeholder (`tanstack`); no setup/check commands are
  declared. Real setup (install/build) and check commands are a pilot gap.
- Pilot: not run — no materialization pilot was executed for this foundation
  in legacy or v1.
- Gaps (explicit): license unverified; pin tag unverified; no setup/checks;
  no pilot; offline-sync/PWA behavior beyond the fixture path unverified.
- Decision/delivery: curated / pilot-ready (downgraded from stable under H3:
  release-grade evidence bars require a Pilot success record, which does
  not exist — see docs/decisions/migration-notes.md).
