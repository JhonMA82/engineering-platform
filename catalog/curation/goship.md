# Curation evidence — goship

- Upstream: <https://github.com/leomorpho/goship>
- License: unverified (gap). The legacy catalog records no license
  observation for this entry; confirm during any future curation.
- Pin: none. The legacy catalog records NO upstream commit for this entry
  (reference-only integration), so no pin is declared and none may be
  invented.
- Legacy review: `platform/boilerplates.json` entry `goship`
  (experimental/catalog-only, tier C, category go-realtime, reference-only
  integration). Recovered by the legacy-identity repair migration exactly
  as experimental knowledge.
- Status: experimental / catalog-only (unchanged from legacy). A catalog
  entry is not a materializable foundation: no adapter is declared, and
  the composer eligibility gate (which excludes experimental decisions and
  requires pin+adapter) keeps this entry out of every candidate and
  materialization pool until a later curation process promotes it.
- Pilot: excluded — `catalog-only` entries require no stable pilot (see
  docs/decisions/migration-notes.md).
- Gaps (explicit): no pin; no adapter; license unverified; not selectable;
  never promoted automatically by migration alone.
